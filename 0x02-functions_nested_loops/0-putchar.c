#include <stdio.h>
#include <string.h>
/**
 * main - prints _putchar
 * Return: ALways 0 (Success)
 */
int main(void)
{
	char putch[50] = "_putchar"
		for (int n = 0, i < strlen(putch), i++)
		{
			putchar(putch[i]);
		}
	return (0);
}
