#include "main.h"
/**
 * mul- function that multiplies two integers.
 * @a: int to mull
 * @b: int to mull
 * Return: multiplication result of the 2 inputs
 */
int mul(int a, int b)
{
	return (a * b);
}
